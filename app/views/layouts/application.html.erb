<!DOCTYPE html>
<html>
  <head>
    <title>JPLDG Blog App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="flex flex-col items-center h-screen">
      <%= render 'shared/navbar' %>
      <div class="container mx-auto px-5 my-2 mt-[70px] md:mt-24">
        <% if notice %>
          <div class="p-4 py-2 text-sm text-green-700 bg-green-100 rounded-lg" role="notice">
            <%= notice %>
          </div>
        <% end %>
        <% if alert %>
          <div class="p-4 py-2 text-sm text-red-700 bg-red-100 rounded-lg" role="alert">
            <%= alert %>
          </div>
        <% end %>
      </div>
      <main class="container p-5 flex-auto"> 
        <%= yield %>
      </main>
      <footer class="w-full bg-gray-900 text-white px-5 py-2.5 md:py-5 flex items-center justify-center mt-8">
        <span class="block text-xs">© 2022 JPLDG</span>
      </footer>
    </div>
  </body>
</html>
