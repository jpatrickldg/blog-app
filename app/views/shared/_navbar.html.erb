<nav class="w-full bg-gray-900 fixed top-0 left-0">
  <div class="container mx-auto">
    <div class="flex justify-between items-center w-full p-5">
      <span class="block text-2xl font-bold text-white">Simple Blog</span>
      <ul class="hidden md:flex items center gap-10">
        <% if user_signed_in? %>
          <li><%= link_to 'All Blogs', posts_path, class: 'block text-white font-medium hover:text-gray-200' %></li>
          <li><%= link_to 'Your Blogs', dashboard_path, class: 'block text-white font-medium hover:text-gray-200' %></li>
          <li><%= link_to 'Write', new_post_path, class: 'block text-white font-medium hover:text-gray-200' %></li>
        <% else %>
          <li><%= link_to 'Login', new_user_session_path, class: 'block text-white font-medium hover:text-gray-200 px-4 py-2' %></li>
          <li><%= link_to 'Sign Up', new_user_registration_path, class: 'block text-white font-medium hover:text-gray-200 px-4 py-2 bg-gray-600 hover:bg-gray-500 rounded-lg' %></li>
        <% end %>
      </ul>
      <% if user_signed_in? %>
        <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: 'hidden md:block text-white font-medium hover:text-gray-200' %>
      <% end %>

      <%# menu %>
      <div class="mobile-menu-button flex items-center md:hidden justify-center p-1 rounded-md hover:cursor-pointer">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-7 h-7 text-white hover:text-gray-200">
          <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
        </svg>
      </div>
    </div>
  </div>
  <%# Responsive navbar %>
  <div class="mobile-navbar p-5 flex flex-col justify-center gap-2 absolute bg-gray-900 w-full -translate-x-full transition duration-300 ease-in-out md:hidden">
    <% if user_signed_in? %>
      <%= link_to 'All Blogs', posts_path, class: 'block text-white font-medium px-3 py-2 hover:bg-gray-700 rounded-lg' %>
      <%= link_to 'Your Blogs', dashboard_path, class: 'block text-white font-medium px-3 py-2 hover:bg-gray-700 rounded-lg' %>
      <%= link_to 'Write', new_post_path, class: 'block text-white font-medium px-3 py-2 hover:bg-gray-700 rounded-lg' %>
      <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: 'block text-white font-medium px-3 py-2 hover:bg-gray-700 rounded-lg' %>
    <% else %>
      <%= link_to 'Login', new_user_session_path, class: 'block text-white font-medium px-3 py-2 hover:bg-gray-700 rounded-lg' %>
      <%= link_to 'Sign Up', new_user_registration_path, class: 'block text-white font-medium px-3 py-2 hover:bg-gray-700 rounded-lg' %>
    <% end %>
  </div>
</nav>