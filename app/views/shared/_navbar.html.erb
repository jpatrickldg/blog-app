<nav class="w-full bg-gray-900 fixed top-0 left-0">
  <div class="container mx-auto">
    <div class="flex justify-between items-center w-full px-5 py-2.5 md:py-5">
      <%= link_to 'Simple Blog', root_path, class: 'block text-xl lg:text-2xl font-bold text-white hover:text-gray-200' %>
      <ul class="flex items center gap-5 md:gap-10">
        <% if user_signed_in? %>
          <li><%= link_to 'Write', new_post_path, class: 'block text-white font-medium hover:text-gray-200' %></li>
          <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: 'block text-white font-medium hover:text-gray-200' %>
        <% else %>
          <li><%= link_to 'Login', new_user_session_path, class: 'block text-white font-medium hover:text-gray-200 md:px-4 md:py-2 py-1' %></li>
          <li><%= link_to 'Sign Up', new_user_registration_path, class: 'block text-white font-medium hover:text-gray-200 px-4 md:py-2 py-1 bg-gray-600 hover:bg-gray-500 rounded-lg' %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>